# ==============================================
#  FFmpeg UI 项目环境配置示例文件
#  请复制此文件为 .env 并根据实际需要修改
# ==============================================

# ==============================================
#  后端配置 (Backend) - 仅后端可见
#  注意：这些变量不能被前端访问
# ==============================================

# JWT 密钥 (强烈建议在生产环境使用至少32字符的随机字符串)
# 可以使用 `openssl rand -hex 32` 生成
SECRET_KEY=your-super-secret-key-here-replace-with-random-string

# JWT 算法
ALGORITHM=HS256

# Token 过期时间 (分钟)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS 允许的源 (用逗号分隔，多个域名之间不要有空格)
CORS_ORIGINS=http://localhost,http://localhost:5173,http://127.0.0.1:5173,https://yourdomain.com

# 后端允许的最大文件大小
MAX_UPLOAD_SIZE=4GB

# ==============================================
#  前端配置 (Frontend) - 前端代码可见
#  注意：这些变量必须以 VITE_ 开头才能被前端访问
# ==============================================

# API 基础路径 (Vite 会读取)
# 开发环境：通常为 http://127.0.0.1:8000
# 生产环境：设置为您的 API 服务器地址
VITE_API_BASE_URL=http://127.0.0.1:8000

# ==============================================
#  硬件加速配置
# ==============================================

# 是否启用硬件加速检测 (true/false)
# 如果设为 false，后端将假装没有硬件加速，强制使用 CPU
ENABLE_HARDWARE_ACCELERATION_DETECTION=true